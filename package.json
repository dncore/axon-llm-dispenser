{
  "name": "axon-llm-dispenser",
  "version": "0.5.20",
  "description": "把用户自有的 OpenAI 兼容网关一键配置到各 Agent 工具",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "vitest run",
    "sync:models": "node scripts/sync-model-meta.mjs",
    "check:models": "node scripts/sync-model-meta.mjs --check"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-autostart": "^2.5.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
